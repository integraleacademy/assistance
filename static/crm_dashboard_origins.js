(function documentDashboardOriginContract() {
  'use strict';

  // Attribution and rendering now live together in crm.js so the dashboard,
  // its tests and its API contract cannot diverge. This compatibility asset is
  // intentionally kept for cached templates that may still request it.
})();
